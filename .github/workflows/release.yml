name: Publish release
on:
  release:
    types: [created]
jobs:
  test-js:
    name: "JS v0.2.2" # v0.2.2 == commit below
    uses: paulmillr/jsbt/.github/workflows/release.yml@4980feb086b99c651ed7ce8d8de637bfb338b83f
    with:
      jsr-slow-types: false
      build-path: test/build
    secrets:
      NPM_PUBLISH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}
    permissions:
      contents: write
      id-token: write
