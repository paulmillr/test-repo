name: Publish release
on:
  release:
    types: [created]
jobs:
  test-js:
    name: "JS v0.2.2" # v0.2.2 == commit below
    uses: paulmillr/jsbt/.github/workflows/release.yml@00cbacd7905ec88896dadc32de8b6710f418c461
    with:
      jsr-slow-types: true
    secrets:
      NPM_PUBLISH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}
    permissions:
      contents: write
      id-token: write
