name: Publish release
on:
  release:
    types: [created]
jobs:
  release-js:
    name: 'jsbt v0.4.5' # Should match commit below
    uses: paulmillr/jsbt/.github/workflows/release.yml@79ad70ab35b88a8b73fee729d6536de0932fd0fa
    permissions:
      contents: read
      id-token: write
