name: Publish release
on:
  release:
    types: [created]
jobs:
  release-js:
    name: "jsbt v0.4.0" # Should match commit below
    uses: paulmillr/jsbt/.github/workflows/release.yml@cde77cab35fb022985c150df5cdbf8baf240e499
    with:
      build-path: test/build
    secrets:
      NPM_PUBLISH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}
    permissions:
      contents: write
      id-token: write
      attestations: write
