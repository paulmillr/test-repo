name: Publish release
on:
  release:
    types: [created]
jobs:
  test-js:
    name: "JS v0.2.2" # v0.2.2 == commit below
    uses: paulmillr/jsbt/.github/workflows/release.yml@0aae74283fe3779b24c6aba41f1208e29bae0219
    with:
      jsr-slow-types: false
      build-path: test/build
    secrets:
      NPM_PUBLISH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}
    permissions:
      contents: write
      id-token: write
