name: Publish release
on:
  release:
    types: [created]
jobs:
  test-js:
    name: "JS v0.2.2" # v0.2.2 == commit below
    uses: paulmillr/jsbt/.github/workflows/release.yml@42ef4eff4a7eae6c0f1d8a3ab20d66c9a8b4985c
    with:
      jsr-slow-types: true
    secrets:
      NPM_PUBLISH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}
    permissions:
      contents: write
      id-token: write
